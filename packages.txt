# apt packages for Streamlit Community Cloud.
#
# opencv-python arrives as a transitive dependency of rapidocr-onnxruntime and
# links against libGL, which is not in Community Cloud's base image. Without
# these the app dies on import with "libGL.so.1: cannot open shared object
# file" -- an error that looks nothing like its cause.
#
# Not needed if you trim the OCR dependencies out of requirements.txt.
libgl1
libglib2.0-0
